# Tasks

- [ ] 在 `saka-format.md` 和 schema 中为 `[[disks]]` 增加 `format`、`storage_mode`、`source_path`。
- [ ] 新增机器包内固定磁盘目录规则：`Disks/`。
- [ ] 把 Runtime 的磁盘路径解析改成按 `storage_mode` 分流，而不是直接信任 `disk.path`。
- [ ] 增加启动前磁盘存在性检查，缺失时在 Sanaka 内先报错，不把问题全甩给 QEMU。
- [ ] 新增“为机器包创建磁盘”的后端接口，默认落到 `<bundle>/Disks/`。
- [ ] 新增“导入外部磁盘”的后端接口，输出标准化外部绝对路径和格式信息。
- [ ] 新增“整理到虚拟机”的后端接口，把外部磁盘复制到 `<bundle>/Disks/` 并改写配置。
- [ ] 未落盘草稿改成记录“待创建/待导入磁盘意图”，而不是提前写假路径。
- [ ] 首次创建机器时，先生成机器包，再物化待处理磁盘到 `Disks/`。
- [ ] 旧配置保存时增加迁移逻辑，把裸文件名迁回新规则；外部绝对路径则显式标记为 `external`。
- [ ] 增加单元测试：路径解析、旧配置迁移、整理到虚拟机、重名避让。
- [ ] 增加手工验收：移动机器包后再启动、导入外部盘、整理后迁移到新机器。
