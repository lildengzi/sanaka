# Tasks

- [ ] 为机器配置新增 `sharing` 数据结构，并保证旧机器配置可自动补默认值。
- [ ] 为 Runtime 增加共享环境检测：是否有可用 `smbd`、当前平台是否支持 SMB 共享主路径。
- [ ] 为 `QemuCommandBuilder` 增加 SMB 共享目录参数拼装，并确保与现有 user networking 合并而不是冲突。
- [ ] 为 `RuntimeMachineState` 扩展 `sharedFolder` 运行态字段。
- [ ] 新增机器共享配置更新接口，支持未运行时直接保存、运行中保存并标记 `pendingRestart`。
- [ ] 为老旧客户机模板预留兼容提示字段，但不阻止用户启用。
- [ ] 增加单元测试：配置默认值、命令拼装、共享环境缺失、运行中修改标记 `pendingRestart`。
- [ ] 给前端写最小对接文档，只说明需要接哪些状态和按钮，不规定视觉实现。
- [ ] 手工验证现代 Windows 客户机能访问共享目录。
- [ ] 手工验证宿主机缺失共享环境时能拿到明确错误提示。
